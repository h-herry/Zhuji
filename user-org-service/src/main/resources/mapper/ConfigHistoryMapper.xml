<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhuji.userorg.mapper.ConfigHistoryMapper">

    <select id="selectByConfigId" resultType="com.zhuji.userorg.entity.ConfigHistory">
        SELECT * FROM sys_user_config_history
        WHERE config_id = #{configId}
        ORDER BY create_time DESC
    </select>

    <select id="selectByUserId" resultType="com.zhuji.userorg.entity.ConfigHistory">
        SELECT * FROM sys_user_config_history
        WHERE user_id = #{userId}
        ORDER BY create_time DESC
    </select>

</mapper>
